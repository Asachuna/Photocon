<div class="row">
    <div class="col-9 col-sm-8 col-md-7 col-lg-6 col-xl-5 col-xxl-4 bg-white border mx-auto pb-5 pt-3">
        
        <h1 class="ps-2 pb-4">ログイン</h1>
        <%= render 'layouts/flash_messages' %>
        
        <div class="row">
            <%= form_with(url: login_path, scope: :session) do |f| %>
                <div class="col-sm-8 offset-sm-2 col-10 offset-1">
                    <div class="mb-4">
                        <%= f.label :email, 'メールアドレス' %>
                        <%= f.email_field :email, class: 'form-control' %>
                    </div>
            
                    <div class="mb-5">
                        <%= f.label :password, 'パスワード' %>
                        <%= f.password_field :password, class: 'form-control' %>
                    </div>
                    
                    <%= f.submit 'ログイン', class: 'btn btn-main btn-lg w-100 fw_bold' %>
                    
                </div>
            <% end %>
        </div>
    </div>
    <div class="mt-4 text-center">
        <p>アカウントを持っていませんか？</p>
        <div class="lead fw-bold"><%= link_to "アカウントを作成", signup_path %></div>
    </div>
</div>
